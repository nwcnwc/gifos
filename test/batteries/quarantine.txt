# QUARANTINE — the only reds the release gate is allowed to tolerate.
#
# One suite per line: <suite-name>  <reason>. The name is the basename without
# .js, matched against the tier/name release.sh reports.
#
# The rules, and they are strict:
#   * An entry here is a behaviour we UNDERSTOOD and DELIBERATELY chose not to
#     fix — the same bar as test/batteries/known-unfixed.sh, which is the prose
#     version of this list. It is never a way to get a release out.
#   * This list may only ever SHRINK. Adding to it is a decision, argued in the
#     commit message, not a convenience.
#   * If a quarantined suite goes GREEN, the gate FAILS on purpose: somebody
#     fixed it, so promote it back into the real gate and delete it from here.
#     A stale quarantine entry silently un-guards working code, which is exactly
#     how the app-in-a-meeting drills rotted.
#   * Starvation is NOT a reason. A suite that fails only on a loaded box is a
#     host problem; re-run it on an idle machine rather than quarantining it.

# (the late-join adoption pair was promoted back 2026-07-28: both ran 3/3
# green on the 8-core box after the 03c local-evidence-phantom fix, which
# removed the NOROOM-cycling that widened the adoption race window. If they
# flake again, that is a REGRESSION to hunt, not a reason to re-add them.)

# (mirror-drill was promoted back into the gate 2026-08-02, cutting 0.9.0. Its
# remaining red was BUILD-TIME CLAIM ORDERING — "E claims a THIRD sdn-class
# announcer as primary and B/F land in the wrong slots" — and this release
# carries exactly that fix: prefCand now matches sdn/sdx on the announcer's
# IDENTITY (x.from === the up-seat/head occupant) instead of on the KEY alone,
# where mosAnn's Map insertion order was literally deciding the winner. The gate
# now requires a quarantined suite to be green on a BEST-OF-3 before it raises
# ESCAPED, and mirror-drill went 3/3. If it reds again that is a REGRESSION to
# hunt, not a reason to re-add it.)

# RESTORED 2026-08-02, same day I removed it — the promotion was PREMATURE and
# that is my error, recorded rather than quietly re-hidden. I promoted on a 3/3
# best-of-3 in the 0.9.0 gate; it then red-twice in the very next gate. Measured
# across the whole day on an idle 8-core box: 3/6, then 3/3, then 0/2, then 4/6
# = 10/17. That is the same coin flip the original entry described, not a fix.
# 3/3 at p~0.55 lands about one time in six, and I took it as confirmation.
#
# NONDETERMINISTIC — see the note in release.sh: ESCAPED is not raised for an
# entry carrying this word, because a best-of-N cannot tell "fixed" from "lucky"
# for a coin-flip suite. Promotion is a HUMAN call; the measured rate above is
# what to base it on. The behaviour is unchanged from the original entry:
# build-time claim ordering, plus a multi-hop demand-wake that lands ~1s when it
# lands at all.
mirror-drill  NONDETERMINISTIC known-unfixed: ~10/17 green measured 2026-08-02. Build-time claim ORDERING + multi-hop demand-wake reliability. Promote only on a deliberate human read of the rate, never on one green gate.

# ADDED 2026-08-05, cutting 0.9.2 — argued, measured, and deliberately deferred.
# The standby-parked leg ("standby pipes are PARKED (~0 B/s)") reds when a woken
# spare re-parks SLOWER than the drill's two measurement windows: forensics show
# the hot standby DECAYING (2332 B/s -> 643 B/s across windows) — the ONE-PIPE
# re-park choreography works but lags. Interleaved 12-run matrix on an idle
# 8-core box, isolating tonight's two product changes:
#   knock-first boot + ghost-law mesh   3/3 red   (both, either law form)
#   knock-first only                    1/3 red
#   ghost-law only                      1/3 red
#   neither                             1/3 red   (the pre-existing flake band)
# So the two changes TOGETHER shift early-room link timing enough to trip a
# wake, and the re-park lag does the rest. The lane is the NEW redundancy/pipe
# machinery (parked spares, mirror hops — post-0.9.1), which a small room never
# touches: no standby pipes exist below stage scale, and tonight's demo-blocker
# fixes (the knock, the ghost law) are exactly what this trade protects. Same
# lane as mirror-drill above and docs/bug-pipe-stg-freeze-2026-08-05.md — one
# cluster, one post-release diagnosis. The drill's other 8 legs stay green;
# NONDETERMINISTIC — promotion is a human call on a measured rate.
#
# THE ENTRY ABOVE NO LONGER DESCRIBES WHAT FAILS — corrected 2026-08-06 from two
# independent boxes (nvidia-laptop gate at 132fa02; raspberrypi, 3 runs + an
# interleaved A/B). The suite is still RED, so quarantine is not masking a fixed
# suite — but do not trust the mechanism written above:
#
#   1. The actual red, in every run on both boxes, is
#        FAIL — found a wake target: receiver holding a relayed stg primary + standby
#      a leg this entry never mentions.
#   2. That leg is a PRECONDITION. test/drills/redun-drill.js:192 guards the whole
#      kill/wake/failover/re-park half behind `if (P >= 0)`. With no wake target
#      the drill runs only ~6 of its ~14 legs — so THE RE-PARK LEGS THIS ENTRY IS
#      ABOUT NEVER EXECUTE. "The drill's other 8 legs stay green" is unverifiable,
#      not confirmed.
#   3. The named leg behaved inconsistently and, when it did fail, failed for the
#      wrong reason: PASSED in one tier pass (stdPipes:1 stdHot:0), FAILED in two
#      re-runs (stdPipes:2 stdHot:1) — but at the PRE-KILL steady-state baseline,
#      before any wake happened, which is not the "a woken spare re-parks slowly"
#      mechanism described above.
#
# So the deferred behaviour is not being measured at all. When the redundancy
# cluster is worked, start at the wake-target precondition, not at re-park lag.
# Neither box gave a clean rate (nvidia drills tier peaked loadavg 10.78/8cpu;
# raspberrypi peaked ~29/4cpu) — re-measure on an IDLE host.
redun-drill  NONDETERMINISTIC known-unfixed: re-park lag behind the drill's windows; 3/3 red only with knock-first+ghost-law combined (1/3 baseline). The redundancy-lane cluster (with mirror-drill + the stg-freeze dossier) is the top post-0.9.2 work item.

# ADDED 2026-08-05 (third entry, same night, cutting 0.9.2) — argued:
# e2e-pipe leg 3 (THE FREEZE SHAPE) reds whenever the encoded-passthrough lane
# actually RUNS — which it only does since ec06c46 gave this suite a real
# engine (the gate pin Chrome 140 has no RTCRtpScriptTransform; the suite now
# takes the newest build, Chrome 149, where the lane is live). The freeze is
# the OPEN, FILED product bug docs/bug-pipe-stg-freeze-2026-08-05.md — proven
# to belong to the lane by an interleaved A/B (3/3 on, 0/3 off, load excluded)
# and explicitly split into its own work item on 2026-08-04. The suite's other
# 17 assertions (module chain, seating, pipe routing, passthrough, no-transcode)
# were green in this same red run — the ONE red leg is the dossier's bug.
# Deliberately deferred WITH mirror-drill and redun-drill: one redundancy-lane
# cluster, one post-0.9.2 investigation. Small rooms never touch this lane.
# NONDETERMINISTIC — the freeze is intermittent per the dossier; promotion is
# a human call after the stg-freeze fix lands.
e2e-pipe  NONDETERMINISTIC known-unfixed: leg 3 is the filed stg-freeze (docs/bug-pipe-stg-freeze-2026-08-05.md), reachable only since the suite got a real engine. Fix rides the redundancy-lane cluster; promote after it lands.
