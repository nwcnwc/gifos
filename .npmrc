# Two exact-pinned dev tools and nothing else (package.json). A lockfile would
# be a third file to keep current for no reproducibility the exact pins do not
# already give, and an untracked one appearing after every install is how it
# gets committed by accident.
package-lock=false
