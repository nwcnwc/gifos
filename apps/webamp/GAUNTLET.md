Winamp 2.9 that lives in a GIF: the playlist, the EQ and the MP3s stay in the file, and one invite shares the setlist and the bands — nothing is fetched, it works on a plane.

## Bars

- **ONE:** Winamp 2.9 / https://webamp.org — the main window, EQ, playlist, vis, hotkeys, skins.
- **TWO:** the playlist and the EQ live in the GIF; works on a plane; invite shares a playlist. Never fetch skins/CDs.

## Rounds

1. Vendor webamp@2.2.0 UMD (MIT). Default base skin is the compiled CSS inside the bundle. No llama MP3, no CDN.
2. Library bytes + EQ + layout in the file. Invite shares playlist titles + graphic EQ. Add URL refused.
3. Icon: Winamp main window playing (vis bouncing, time ticking). Cover: the real first-boot window.
4. First open is a player: original **Intro / Green LED / On a Plane** (synthesized PCM, not Nullsoft's llama), a smile EQ, and Play. EQ is a first-class `prefs.eq` field; marquee ticks no longer starve the write. Invite setlist paints from the last mix + other people (self is not a guest). Cover is a photograph of `#main-window` + EQ + playlist with those three tracks.

## Remaining gap

Webamp.org still has Milkdrop. This copy ships the classic vis (spectrum / oscilloscope) — authentic Winamp 2.9, not the plugin. User skins restore via a simulated drop; if that misses in a given browser, drop the .wsz again.

**READY-TO-SIGN**
