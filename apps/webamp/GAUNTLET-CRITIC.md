# Webamp — gauntlet critic

Blind A/B against Winamp 2.9 / https://webamp.org (Jordan Eldredge). Played the original in Chromium and this port from the **NEW** `site/apps/webamp/webamp.gif` (639 683 bytes, mtime 2026-08-31T23:35:54Z) installed into the real GifOS sandbox (`run.html#id=`, `window.gifos.db` present). One browser. Desktop 1280 and phone 390 / 360. Listing read on `/store.html#app=webamp`. Search typed on `/store.html`. Icon frames read at 128px and at real Home Screen size (64px, next to Welcome.gif / Camera.gif).

**winner:** OURS

**single biggest remaining gap:** webamp.org is still a party — Llama Whippin' Intro in the marquee, 13 named tracks (43:40), a Milkdrop window, and a row of `.wsz` files on a Winamp 98 desktop — and it opens onto that with nothing in the way. This copy's first paint is an OS sheet, "Webamp would like to… Open a web link in a new tab", for a capability the app never calls (`capabilities.links` / minBuild 2154; zero `gifos.open` / `gifos.launch` in the tree). After Confirm you get Intro / Green LED / On a Plane: 19 seconds of synthesized PCM and the classic vis. The file-is-the-save reason is now true; the first minute is not a reason to leave webamp.org.

**would a stranger who knows the original use this copy:** "I would use the GifOS one on a plane, or to hand someone the file with the songs inside — webamp.org is a tab that fetches `llama-2.91-*.mp3` and a skin museum. Close this, come back, the same three tracks and the EQ I just set are still there. I will click through the unused-link sheet once. I will not stay for the demo: it is 19 seconds of oscillator, not Llama. Drop my own MP3s and the GIF is why I'm here."

**HARD WALL:** none that I could break. Runtime wall is clean: first-open requests all same-origin, zero skin/CDN/unpkg/jsdelivr/llama fetches (`connect-src 'none'` on the srcdoc). No `localStorage` (sandbox throws). `COPYING-webamp.txt` is packed. Library is private. Signed by gifos.app. `site/apps/index.json` lists `webamp` (201 apps). Store search for "webamp" returns the card, not "Nothing matches that." Listing claims that were checked: unofficial, Jordan Eldredge, MIT, no llama MP3 shipped, three original demo tracks, smile EQ, EQ persist, invite setlist paint. "Opens web links" on the listing abilities row is the unused-capability overclaim — a failed honesty note, not a sandbox break. Two-tab mesh join minted a `/run.html#j=` link then the renderer died on this box; the setlist paint path was verified by writing a live peer row.

## Face (always judged)

- **Icon (64px):** A grey Winamp main window, vis bars bouncing, time ticking `1:20`→`1:22`, play button lit. The loop demonstrates. At real Home Screen size next to Welcome.gif / Camera.gif it still reads "player" — WINAMP title is a speck, the green vis and transport row are what land. Original has no animated icon. Structural win.
- **Store art:** `cover.jpg` is a photograph of `#main-window` + EQ + playlist (1200×879): GifOS — Intro playing at `00:01`, smile EQ, three named tracks at 0:06 / 0:06 / 0:07. It matches the running window after Confirm (same chrome, same tracks, same curve). Beside 2048 / Flexbox Froggy / JS Paint this is the actual product mid-use, not a mockup. Grid-card crop is heavy on the empty playlist black; the left rail still reads Winamp. No shell toolbar in the frame.
- **Listing copy (read on the rendered store page):** Tagline *Winamp 2.9 that lives in a GIF — playlist, EQ, and your MP3s stay in the file, even on a plane.* Description leads with "It opens playing" and the three demo tracks, then Invite, then the three windows, then unofficial / no llama. That is the right shape. Claims I could check are true of this GIF except: "the first screen is a player" is true of the app and false of the first paint (the unused-link sheet sits on top, on desktop and on 390); the abilities row prints **Opens web links** for a call the app never makes. 625 KB, signed, Media / Creativity, Works offline.

## Product notes (not the gap, but they sit on the table)

- First open of a cold GIF is a player. Playlist: **GifOS — Intro / Green LED / On a Plane** (6.2 / 6.2 / 7.2 s, `audio/wav`, synthesized in-file). Status **PLAYING**. Vis bouncing. EQ on, auto off, 60 Hz = 78 (smile). Hint: "Demo setlist is in this file. Drop MP3s to add yours." `#setlist` and `#room` hidden — self is not a guest. That is the last round's empty-Winamp hole, paid.
- EQ persist is real. Dispatch 60 Hz → 100 wrote `prefs.eq.sliders[60] === 100` with `seeded: true` and a 3-row `list`. Reload of `run.html#id=` restored live 60 Hz = 100 and the same three titles, in that order.
- Invite setlist paints. Injecting a `kind: 'peer'` row + `mix` with those three titles unhid `#setlist` (`ROOM PLAYLIST` / GifOS — Intro / Green LED / On a Plane) and `#room` ("2 in the room"). Invite is OS chrome. A join link was minted (`#j=` + local relay). Guest convergence was not photographed — the box dropped the second context.
- The three windows *are* Winamp 2.91. Main 275×116, EQ 275×116, playlist beside on desktop, stacked 275-wide on a 390 phone (`body.phone.scroll`). Title bar, vis, marquee, volume/balance, EQ graph, presets, ADD/REM/SEL/MISC — same engine as the original. Milkdrop is off; authentic 2.9, and a reason the original still looks more alive. `handleAddUrlEvent` refuses fetch.
- Phone stack (main / EQ / playlist, dock with + MP3s / Skin) is usable and better than an unstacked 2.9 layout on 360. The buttons are still 2.9 pixels; a finger wants the dock, not shade/close. Hint truncates. The same unused-link sheet covers the whole phone on first open.
- No llama, no CDN. webamp.org's first paint fetched `https://webamp.org/assets/llama-2.91-*.mp3` (12 requests on that load). This GIF: 0 foreign.

The empty-player / dead-EQ / hidden-setlist / fake-cover round is done. Drop the unused `links` tax so the first screen is the window the cover already shows, and give the demo a reason to last longer than 19 seconds. Until then bar two is why a stranger would switch, and bar one is still webamp.org's party.
