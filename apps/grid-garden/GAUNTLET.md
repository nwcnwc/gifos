# Grid Garden — gauntlet

**Win:** A stranger who knows cssgridgarden.com uses this copy because the file is the save (close it on a plane, same bed) and one invite puts a friend on the same plot with the same CSS — the original needs the site and localStorage.

## Bars

- **ONE:** https://cssgridgarden.com (Thomas Park). Floor, not ceiling. 28 levels, carrot/weed art, Next when the beds match.
- **TWO:** Offline; progress in `gifos.db` so the GIF is the save; Invite shares the garden (level + CSS) with no server.

## Rounds

1. Vendor English levels + original art; drop jQuery, webfonts, analytics, ads, hash-language. Phone stack. **Gap closed:** it runs in the sandbox.
2. Persist level/answers/solved in a private collection. **Gap closed:** close and reopen, same bed.
3. Shared plot on Invite. **Gap closed:** two tabs, same CSS, same level.
4. Icon waters a carrot on a 5×5 bed (the first lesson). Cover is mid-level 16: a wide watered carrot and the editor showing `grid-area` (glyph set completed so LEVEL / WATER / NEXT read). Listing leads with offline + invite.

## Remaining gap

Translations from the original (~40 languages) are not shipped — English only, to keep the GIF small. The original's other-games ads are gone on purpose.
