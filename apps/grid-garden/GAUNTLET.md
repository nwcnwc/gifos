# Grid Garden — gauntlet

Critic (blind A/B vs cssgridgarden.com) picked **COMP**: spanned beds stretched the whole carrot sheet (`background-size: 100% 100%`) instead of tiling one cell, so level 16 was two giant carrots over a puddle. The bed now tiles at `--cell` (board/5, `20cqw` fallback) with `background-repeat: repeat`, matching the original field of carrots. Cover retaken from the real window at level 16.

**Win:** A stranger who knows cssgridgarden.com uses this copy because the file is the save (close it on a plane, same bed) and one invite puts a friend on the same plot with the same CSS — the original needs the site and a tab left open.

## Bars

- **ONE:** https://cssgridgarden.com (Thomas Park). Floor, not ceiling. 28 levels, carrot/weed art, Next when the beds match.
- **TWO:** Offline; progress in `gifos.db` so the GIF is the save; Invite shares the garden (level + CSS) with no server.

## Rounds

1. Vendor English levels + original art; drop jQuery, webfonts, analytics, ads, hash-language. Phone stack. **Gap closed:** it runs in the sandbox.
2. Persist level/answers/solved in a private collection. **Gap closed:** close and reopen, same bed.
3. Shared plot on Invite. **Gap closed:** two tabs, same CSS, same level.
4. Icon waters a carrot on a 5×5 bed (the first lesson). Listing leads with offline + invite.
5. Tiled the carrot/weed/water/dirt sheets to one cell (`--cell` = board/5). Cover is a real 1200×720 window at level 16 — a 4×3 field of carrot pairs under water, not two giants.

## Remaining gap

Translations from the original (~40 languages) are not shipped — English only, to keep the GIF small. The original's other-games ads are gone on purpose. The icon carrot is still a speck at 64px.
