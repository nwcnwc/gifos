# Carbon gauntlet

**Win:** A stranger who knows carbon.now.sh uses this one because there is no account, the snippet and the theme live in the file, export is a PNG from here in Hack, and one Invite lets a friend land on this image and type on it.

**READY-TO-SIGN**

## Bars

- **ONE:** [carbon.now.sh](https://carbon.now.sh) / [ray.so](https://ray.so) — the window, Seti, the grey field, traffic lights, theme/language/background, PNG, Hack at 14px.
- **TWO:** no account; snippet+theme persist in the GIF; PNG from the sandbox; Invite edits the same snippet.

## Pieces

| piece | bar | status |
|---|---|---|
| ICON | reads as a code window at 64px; animation types syntax bars | in |
| STORE ART | mid-use Seti + pluckDeep on the grey field | in |
| LISTING COPY | leads with no-account / file-is-the-save / invite | in |
| Window chrome | traffic lights, Seti default, grey bg, padding, shadow, Hack 14px | in |
| Themes | Carbon's own tables from constants.js | in |
| Export PNG | canvas at 1×/2×/4× in vendored Hack (not Liberation/Menlo) | in |
| Recents + persist | gifos.db save + recents | in |
| Invite | guest opens onto the host snippet; last write wins | in |

## Rounds

1. First build: themes vendored, tokenizer, Carbon chrome, PNG export, recents, invite.
2. Cover and icon judged against carbon.now.sh's own grey Seti shot — default snippet, not an empty boot.
3. Phone: toolbar wraps, stage scales the window, settings/recents are a sheet, Back closes the sheet.
4. Hack Regular + Italic packed inside the GIF (data:font/woff2, COPYING-hack.txt). Window and PNG use it. Guest join no longer publishes the default sample over the host.

## Remaining gap

Upstream's CodeMirror (70+ modes, per-range bold/italic marks) is not in the sandbox; highlighting is a local tokenizer on Carbon's own theme keys. The typeface is no longer the gap.
