# Algorithm Visualizer — gauntlet

**Win:** the original needs a compile server and an account; this GIF plays the tracers on a plane, keeps the last algorithm inside the file, and an invite is follow-along.

## Bars

- **ONE:** algorithm-visualizer.org — dark IDE, Chart + Array + Log, blue select / magenta patch, Play / Prev / Next / Speed.
- **TWO:** offline (no server), last algorithm+input in `gifos.db` (the file is the save), invite is host-driven follow-along.

## Catalog (trimmed on purpose)

Nineteen walk-throughs. Quality of playback over the full upstream tree (C++/Java + compile server cannot run in the sandbox).

Sorting, search, graph, DP, backtracking. Tracers: Chart, Array1D, Array2D, Graph, Log.

## Rounds

1. Tracer engine records `delay()` chunks; player seeks; sorts finish sorted (build smoke).
2. Cover is mid-quicksort, not empty boot. Icon is six bars swapping.
3. Follow-along: host writes `session` (read-only); guests rebuild the same trace from the input and seek the cursor.
4. Phone: ☰ catalog, Back closes it.

## Remaining gap

No live code editor — the original's "visualize your own C++" needs the compile server. Curated JS walk-throughs plus editable input is the offline product.
