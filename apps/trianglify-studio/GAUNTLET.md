A stranger who uses trianglify.io would pick this because the wallpaper they made is still there after they close the tab, it works on a plane, full-size PNG and SVG are free, and an invite is the same seed on a friend’s screen.

**READY-TO-SIGN**

## Bars

- **ONE:** [trianglify.io](https://trianglify.io) — the author’s GUI. Live preview, palettes, cell size, variance; high-res downloads are paid; needs the network; forgets you when you leave the tab.
- **TWO:** offline; last palette+seed live in the GIF; one invite shares the seed; a link can open onto a seed (`go.seed`); PNG/SVG at wallpaper size with no account.

## Rounds

1. Vendor trianglify@4.1.1 UMD (GPL-3 inside the GIF). Studio chrome: palettes as colour chips, memorable seeds, mesh/scatter, Linear/Sparkle/Shadows, wire, size, PNG/SVG.
2. Persist last wallpaper in the file; restore without rerolling. Invite shares the whole recipe on each player’s own row.
3. Icon: a low-poly card whose triangles hold still while the wash of colour cycles — readable at 64px as “triangle wallpaper.”
4. Cover: mid-use Spectral wallpaper with the seed, chips and export row, not an empty first boot.
5. Listing leads with the file-is-the-save and free full-size download, not a feature list.

## Remaining gap

4K with a very fine mesh still takes a couple of seconds to encode on a phone — same cost as painting that many triangles anywhere; HD is instant, and the slider’s floor stops a 4K render from becoming tens of thousands of cells.
