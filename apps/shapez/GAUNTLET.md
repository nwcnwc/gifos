# Gauntlet — shapez.io

**Win:** A stranger who knows shapez.io uses this copy because the
factory lives in the GIF and one invite is co-op — the original tab
forgets you, and it never had a friend on the same belts.

Bar ONE: shapez.io / Factorio (the named original; Factorio is the
genre ceiling for feel).
Bar TWO: the factory is the file; invite is co-op; GPL-3 packed.

## Round 1 — first playable slice

- Extractor, belt, cutter, rotator, painter, trash, hub.
- Levels 1–8 of the opening (circles → halves → rectangles → rotate →
  quarter → red circles → blue half-rectangles), then freeplay.
- Save in `gifos.db` (`cells` + `world`).
- Host simulates items; guests see `flow`; both place on `cells`.

## Remaining gap

Full 26-level campaign (stacker, mixer, balancer, tunnels, wires) is
out of scope for this GIF. The opening must still *feel* like placing
the first belt in shapez — patches readable, machines obvious, clogging
honest, the hub hungry.

## Always judged

- ICON: belt carries a circle, cutter splits, painter turns it red.
- STORE ART: mid-factory around the hub, items on belts, a friend cursor.
- LISTING: leads with “the factory lives in the GIF / one invite is co-op”.
