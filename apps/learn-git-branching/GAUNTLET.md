# Learn Git Branching — gauntlet

**Win:** A stranger who knows pcottle.github.io/learnGitBranching uses this copy because the file *is* the save and one invite puts a friend on the same lesson, offline.

## Bars

- **ONE:** https://pcottle.github.io/learnGitBranching/ — the original visual git trainer (36 lessons, Raphael graph, command prompt). Floor, not ceiling.
- **TWO:** Offline; progress in `gifos.db` inside the GIF; invite = two people on the same level, first to match the goal; no account.

## Rounds

1. **Engine.** Vendored English lessons + a headless reimplementation of the original git/treeCompare semantics. All 36 official `solutionCommand` lines match their `goalTreeString`. Remaining gap was apostrophe tokens (`C2''`) and resolving `HEAD~2` after checkout during rebase — both fixed.
2. **Product.** Command box + graph on a phone stack; shortcut chips; lesson slides; interactive rebase sheet; sandbox. Origin graph when remotes exist.
3. **Icon / cover / listing.** Sticker graph that grows a branch. Cover is a merge in progress, not an empty boot. Listing leads with offline + the file is the save + invite race.

## Remaining gap

The original's Raphael animation (commits sliding as they rebase) is still a snap-to-new-tree here; the graph is correct, the motion is not as theatrical.
