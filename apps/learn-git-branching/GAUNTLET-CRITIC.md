# Learn Git Branching — fresh-eyes critic

Bar ONE: [learngitbranching.js.org](https://learngitbranching.js.org/) (`?NODEMO`). Bar TWO: GifOS — offline, progress in the GIF, one Invite is a same-lesson race, no account. Driven in one Chromium against the packed GIF at `site/apps/learn-git-branching/learn-git-branching.gif` (207646 bytes, sha `e0b85ceb…`), desktop 1100×820 and phone 390×844, two contexts through the local relay (`ws://127.0.0.1:8790`), store listing at `/store.html#app=learn-git-branching`, Home Screen at 64px. Comp opened live.

**Winner: OURS**

**Single biggest remaining gap:** The commit graph still loses the glance test, and on remotes it loses the *read* test. Git Fetchin' piles C3 on top of `o/main`; `bugFix`, `o/bugFix` and HEAD stack into one blob; newest grows *up*; there are no parent arrows; every `render` snaps. The original exists so you *see* local versus origin. This copy can run `git fetch`. It cannot show it. The store cover is a drawing of unlabeled blue/green/orange blobs that is not this magenta C0–C3 window.

**Stranger-reason-to-use-this:** Close it, the checkmarks live in the file. Invite puts a friend on the same lesson without them pressing anything. Both ran. Ada solved intro (`Solved in 2 commands (par 2)`); reopen listed `Introduction to Git Commits ✓`. Ada pressed Play a friend on **Branching in Git**, then Invite; the OS minted `run.html#j=wqt5jbzzss&relay=…`; Sam opened the link and **did not tap**. Sam landed on Branching in Git, friend-bar up, slides closed, `Same lesson: Branching in Git. First to match the goal wins.`, scores `Sam (you) · 0 cmds` / `Ada · 0 cmds`. Sam's `git commit` became `Sam · 1 cmds` on Ada's bar; Ada's tree stayed C0–C1. That is why you would say this version out loud. The original already remembers the tab. It cannot do that race.

**Wall breaks:** none this round. `site/apps/index.json` lists the slug (201 apps). Store search `git` and `branching` both return the card; Learning shows it. No off-origin `fetch` from the host run (the packed JS has none — `git fetch` is the simulator). `#solved-banner` stays `display: none` until the tree matches. `minBuild` 947. Signed as gifos.app. 203 KB.

---

## Icon

Home Screen, 64px, next to Welcome / Camera. Dark rounded sticker, two blue nodes and a stem — it reads “commit graph” at a glance. The loop that grows a side branch still earns the frames; at this size the yellow tag is a smudge. Fine. This drive placed it at 48,48 on top of Welcome (`NEW` / `v1.0.0` / `Git` pills). That is `saveItem`, not the sticker.

## Store art

Listing hero is `cover.jpg` from the drawn `screenshot.png`, 1200×720. Mid-use *idea* is right: a merge, a tiny goal, a command row.

The picture is a different app. Unlabeled blue/green/orange blobs, empty grey chips, a command bar that is a rectangle. The running window is magenta C0–C3 with `main` / `HEAD` pills and a real `$ git commit`. Chess Grandmaster's cover is a photograph of the thing you get; DigitalJS sitting next to this card on a `git` search is a photograph of a circuit. At card size (~240×144) it is a mute graph that could be any dag tool. At hero it is a toy ROM of a UI this GIF does not paint. No `coverCrop` — there is no GifOS chrome in the drawing, so the crop is not the problem. The drawing is.

## Listing copy

Rendered on `/store.html#app=learn-git-branching`. Tagline: “The git trainer that lives in a GIF — close it, come back, still on the same commit.” Description leads with offline + the file is the save + Play a friend then Invite. Then solo LGB, 36 lessons, phone chips, unofficial-port honesty. Porter / basedOn / blessed:false / MIT / signed gifos.app all show.

Claims that were true in this run: type real git, 36 lessons in the sheet (Sandbox through Pull arguments, intro marked ✓ after solve), hint/undo/reset chips, phone stack, close-and-reopen still carries the checkmark, no account, **and the invite race**. Last round the invite sentence was an overclaim. It is not one now.

## Product

### Typing git

The box is a real prompt. Two `git commit`s on intro printed `Level solved! Goal tree matched.` and `Solved in 2 commands (par 2)`. Shortcuts fill the box (`commit` chip wrote `git commit` on the phone). Undo/reset/hint/lessons are on the row. Usable. The original’s terminal is still better: a Mac window, one command per line with a check, `main*` + HEAD on the graph as you type.

### Commit graph

Original after boot (`?NODEMO`): cyan field, teal commits growing **down** from C0, black arrows, `main*` and HEAD on C1. Ours: dark field, magenta circles, newest **high**, a stem and no arrows, HEAD as a yellow pill on C1 (that last part now works — last round HEAD never appeared while it pointed at a branch).

Git Fetchin' is the tell. Local shows C0–C3, `main`, `o/main`, `bugFix`, `o/bugFix`, HEAD, a “Local” title. C3 sits **on** `o/main`. The Goal inset is the only place the remote topology is actually legible. `origin-wrap` is un-hidden; a second origin tree does not occupy the frame. Every command still snaps the SVG.

The Staging Area now has two lists. Boot: Working Directory `app.js` / `styles.css`, Staging Area `empty`, graph still C0–C1. `git add app.js` moves the name. Goal already shows C3 with the file names on the commits. That is a command runner with a file list, not the original’s working-directory → staging-area → commit picture. It is no longer *nothing*.

`#solved-banner` is `[hidden]` and `display: none` on first boot, after skip, on Staging Area, on Git Fetchin', and on the guest who just arrived. After the two-commit match it is a green **Solved ✓ / Next lesson**. Last round a stranger’s first frame was that they already finished. That lie is gone.

### 36 lessons

The Lessons `<select>` lists 36 ids plus Sandbox — the English corpus, original names (“Detach yo' HEAD”, “Git Fetchin'”). Slides are the original teaching markdown (Git Commits 1/3 on first boot; Skip works). Sandbox is first in the list.

What the original still has on the same corpus: Main/Remote tabs, numbered 1–4 buttons, Solution, Objective, 20+ locales, Hg, level builder, shareable `?command=` URLs. And a graph that can teach the remote lessons without a pile-up.

### Progress in the GIF

True. After solving intro and moving on, a later open of the same file still had `Introduction to Git Commits ✓` in the lesson list. Original keeps that in the origin’s localStorage; it does not travel with the trainer.

### Invite race

OS Invite works (link `run.html#j=…&relay=ws://127.0.0.1:8790`). The in-app race now works through the remount.

Host: Skip, switch to Branching in Git, **Play a friend** (`Waiting for a friend…`, `Ada (you) · 0 cmds`, `racing: true`), then **Invite**. After the room flip: friend-bar still up, still Branching in Git, still waiting, Ada still lead. Guest opened the link and tapped nothing. 2.5s later: live, friend-bar up, Play a friend hidden, lesson sheet closed, `levelId: intro-branching`, status “Same lesson: Branching in Git. First to match the goal wins.”, both names. `gifos.info()`: Ada `owner: true`, Sam `owner: false`. Players rows: Ada `lead: true`, both `levelId: intro-branching`. Sam typed `git commit`; Ada’s scoreboard went to `Sam · 1 cmds` while Ada’s tree stayed C0–C1 and Sam’s grew C2. Trees are private; the race is the scoreboard. I did not run either side to a match, so the crown line was not on screen. The join the last round failed is the join that ran.

### Phone (390×844)

The stack is the right shape: graph, tiny Goal, chips, `$` box, Run. First boot is the lesson card filling the screen (Skip / Next are thumbable). A chip wrote `git commit` into the box. Playable. Goal is a stamp. Original is a desktop cyan canvas; this is the one you can actually type on a phone.

### Offline

Host run issued zero off-origin requests. A plane is true of a tab you already opened. The store card is findable now, so a stranger can get that tab.

## A/B

Put a stranger who knows Learn Git Branching in front of both.

Original: cyan tree, HEAD on the branch, commits grow down with arrows, a terminal, 36 lessons, working-directory sidebar, it remembers the tab, 20+ locales. Git Fetchin' is a picture of two graphs.

Ours: a magenta snap-tree that now paints HEAD, hides Solved until it is true, lists staging files, keeps checkmarks in the GIF, and — this round — puts a guest who opens the Invite on the host’s lesson with no tap. The store card is in the catalog. The cover is still not the window. Remotes still collide.

They have a reason to use this one: the file *is* the save, and one link is a same-lesson race, offline, no account. They will still wish the tree moved like Raphael, and they will not learn fetch from the local pane. Until the graph can hold a remote lesson and the cover is a frame of that, that is the climb. The race that used to be the reason to call COMP now runs. OURS.
