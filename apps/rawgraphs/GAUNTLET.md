# RAWGraphs — gauntlet

**Win:** the original is a website; this GIF is the table — close it, the last dataset and mapping are still there, and one invite is two people on the same chart, fully offline.

## Bars

- **ONE:** rawgraphs.io / Tableau — paste a table, pick an alluvial or a treemap, map columns onto visual variables, export SVG.
- **TWO:** the dataset lives in the GIF (`gifos.db`); works offline; invite is live collab on the same mapping.

## Catalog (trimmed on purpose)

Twelve visual models. Quality of the load → map → draw loop over the full 30+ React catalogue (Excel import, hexbin, voronoi-treemap, and d3 UMD `Function()` at load cannot run in the sandbox).

Alluvial, treemap, bar, stacked bar, line, bubble, pie, bump, beeswarm, sunburst, circle packing, streamgraph.

## Rounds

1. Sample prize films open as an alluvial (origin → studio → genre, sized by box). Parse + draw smoke in `build.mjs`.
2. Cover is that alluvial mid-use, not empty boot. Icon is a grid becoming flows.
3. Last CSV + mapping restore from `save`. Play together shares `room`.
4. Phone: Data / Type / Map / Chart tabs; Back steps out. Excel files get a plain refusal.

## Remaining gap

Not the full RAWGraphs chart catalogue, and no `.xlsx` importer — save as CSV first.
