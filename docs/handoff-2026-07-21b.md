# Handoff — 2026-07-21b (R5 browser drill; H-CHAIN column; loss pin)

Written cold. Continues `handoff-2026-07-21.md`. **Tip of main:** `047e58e` (after column-clique + gossip fix).

---

## One-sentence headline

**R5 browser pick-one drill green**; **H-CHAIN S1 column-clique LIVE** (sim + mesh.js, leg F, Q5 column audit, sweep green); **loss-wedge 10% repro re-confirmed** (seated=5/60, phantom `/1.0=6`) and pinned in `sim/repro-loss-wedge.sh` — three-state implement still open.

---

## Done this session (on `main`)

| Commit | What |
|---|---|
| `8322efb` | `test/drills/e2e-r5-fork-pick.js` — same-key dual greeter → `#fork-modal` → pick-one → no bridge. Reuses `forceSeat` / `__gifosBlockIce` / `stageForTest`. Unit clustering stays `mesh/r5-fork-pick.js`. |
| `06b3cd9` | H-CHAIN S1 column-clique: admit + reactive heal + childless column scooch; `repro-hchain` leg F; Q5 column masks; laws/roadmap. |
| `047e58e` | Fix: left-pack must stay FINDLEAF-then-scooch (column scooch-first only). Left-pack scooch-first regressed gossip after 40%-kill to 298/299. |

### Gates green after column work
- `sim/repro-hchain.sh` A–F GREEN (leg F: column-mate heals `/0.1` within 40 ticks)
- `sim/repro-headless-row.sh` GREEN
- `sim/sweep.sh` quick GREEN (churn 30/30, split, D5)
- `test/mesh/q5-designation.js` row+column ALL PASS
- `test/mesh/mesh-harness.js` ALL PASS (incl. gossip after 40%-kill 299/299)
- `join.sh --quick` on nvidia: 11/12 after `06b3cd9` (harness gossip flake from scooch order); fixed by `047e58e` — re-run if you want belt-and-braces

### R5 browser drill
```bash
ssh nvidia-laptop
export PATH="$HOME/.nvm/versions/node/v22.23.1/bin:$PATH"
export MEET_CHROME=/opt/google/chrome/chrome
cd ~/projects/gifos && git pull && node test/drills/e2e-r5-fork-pick.js
```

---

## Loss wedge — repro re-pinned (not fixed)

```text
net loss=0.10, joinmode burst, init 60, converge 400000
STATE seated=5/60 dups=0
SEAT 0 … /1.0=6   ← phantom
SEAT 6 state=searching forever
```

Script: `sim/repro-loss-wedge.sh`
- A) healthy burst N=60 (hard gate)
- B) loss=0.10 N=60 (OPEN until three-state; documents wedge)
- C) healthy N=100 (hard gate)

**Implement next (design LOCKED — do not re-litigate):**
1. Soft `sitting-down` on admit (assigner writes; not greeter)
2. Joiner `take`/HELLO upgrades to `seated` only
3. Assigner recheck + soft TTL
4. Row fill while head sitting-down OK; **depth/children only after head seated**
5. Never re-try firstHandLive hand-off gate or PLACE TTL alone
6. Pin: loss B ≥55/60 seated; healthy A/C not regress; then port mesh.js

---

## Still NOT done (priority E→D→A→B; E/D mostly done)

1. **Loss wedge three-state** (A) — design locked, pin script ready, implement sim-first
2. H-CHAIN remainder: self-wire-with-hint; deeper vertical
3. Optional: add `e2e-r5-fork-pick.js` to `join.sh` (left standalone like peer-relay-reunion)
4. Roadmap leftovers: x402, e2e-video via Hub flake, etc.

---

## Operating (unchanged)

- nvidia for browsers; node 22; `MEET_CHROME=/opt/google/chrome/chrome`
- `RELAY_DEV=1` multi-socket drills
- Before push of `meet.html` / `mesh.js` / `mesh-wire.js`: `join.sh --quick` on nvidia
- Commit early/often on main + push
