# Handoff 2026-08-05 (morning) — 0.9.2 shipped; the V4 mint is half-diagnosed

Session-local state written down so the next session starts at speed.
Everything is committed and pushed through `c14fc87`.

## What shipped tonight (context, not work)

**v0.9.2 (build 1039) is LIVE** — cut from a gate reading GREEN 124 / FLAKY 0
/ RED 0 / DEAD 0 / QUARANTINED 4, live-verified cross-box on prod (joiner
seated t+0, video by t+3s). The demo-night failure was three stacked bugs,
all fixed at the root:

1. **THE KNOCK WAITS FOR NOBODY** (`3dd8802`, `d7a44ff`): getUserMedia no
   longer gates joinRoom(); media attaches via the shared attachLocalTracks
   path; a cam tap during the pending boot ask JOINS it (bootGumPending),
   never races it.
2. **A GHOST PHONE TARGET MUST BE FALSIFIABLE** (`0c7f93d`→`ec06c46`,
   horizon 90, narrowed to NEVER-heard inheritance ghosts): one peer's link
   blip can no longer E1-suicide a healthy bystander. Both twins. The
   narrowing matters: the broad form minted dups under mass-kill and killed
   severed-but-alive neighbours under adversary churn — both measured, both
   in the commit messages.
3. **The pinned chromium (1193) has NO H.264** — the meet.js talking-head
   shim hung forever on it and faked a night of cross-box "flakes"
   (behavior 24a). Shim now degrades to the portrait canvas (`3108bb2`);
   trap recorded in test/README.md ("Playwright chromium builds may lack
   H.264") and the gate-hosts memory.

Also: 24a's clear-probe and stadium-dup's fixture got forensic upgrades
(print terms on failure; re-manufacture on rotation; 150s ring-window wait).

## THE LIVE FRONT: V4 dup-mint — the mechanism is named, the fix is not built

The morning experiment (the handoff-2026-08-04 spec, executed) landed the
admit-site ledger: `ADMIT_SITE`/`ADMIT_LOG` in the sim, DUPMINT prints its
minting call-site, 03c clears print as KNOCKCLR/PHANTOMCLR. N=3000 det,
converge 600 (`MESH_DUPLOG=1`, ~30s):

    DUPMINT 1791   KNOCKCLR 46   PHANTOMCLR 5
    sites: deep-firstFree 1772 · s1-designated 17 · s1-headless-soft 2

**Verdict 1 — knock-is-evidence is REFUTED** (third refuted hypothesis in
the audit doc; do not re-derive any of them).

**Verdict 2 — the mint is TWO LIVE ADMITTERS FOR ONE CELL.** First mints are
all s1-designated: e.g. t=27 cell 0/1.4 — incumbent seated@16 by placer 0,
re-admitted@19 by placer 5. The H-CHAIN DEVOLUTION arm fires on a
propagation-lagged read (a peer briefly reads the designated admitter's cell
as empty/phantom) while the designated admitter also serves. The duplicated
S1 parents then hand identical owned child rows downward and deep-firstFree
amplifies ~100x. This is the audit doc's suspect (c) with birth certificates.

**Next experiment (ready to run):** log the exact `cellReserved` read at the
devolution decision (which guard passed: no occ? not firstHandLive? phantom?)
and whether every seed pair is devolution-vs-designated or ever
designated-vs-designated across a healTry window. Then the fix shape:
**devolution must be gated on CONFIRMED absence, not a lagged view** — the
same evidence-class discipline as the ghost-falsification law (probe-gated,
first-hand). Sim-first, BOTH twins (site/js/mesh.js + test/sim/mesh.cpp +
mesh_seat.inc — the twins contract). Done means: **N=5000 converges** AND
mesh-harness + churn-combos + hchain + compaction + adversary + atomic-move +
ghost-join + headless-row + join-patterns + loss-wedge + c-sweep all green.

Then, in order (audit doc's own sequencing):
1. Permanent sim gate at N≥5000 (today's gates stop at N=800/1000 — an
   untested N is a dead suite wearing a green badge).
2. healing-laws.md: the scalability law, the digest law, AND the
   ghost-falsification law from tonight (it's live in both twins, unrecorded).
3. V1 rollups (design sketched in full in the audit doc, incl. migration
   table) → V2 statusOf cap → V3 dial gate. The O(N) status flood is STILL
   LIVE — nothing in 0.9.2 touched it.

Tools that exist now: `MESH_TRACE=<id>` (per-seat forensics: join/resumeAsk/
REQUEUE-with-reason/TAKE/phoneHome), the admit-site ledger, MESH_DUPLOG.
Sim REPL: `./mesh-sim --service`, verbs det/seed/init/joinmode/converge/
state/dups/bad/seat/hist/relay/sever/killat/find/compact/net.

## The quarantine cluster (4 entries, one investigation)

`test/batteries/quarantine.txt` holds four argued entries — mirror-drill,
redun-drill (standby re-park lag; 12-run interaction matrix in the entry),
e2e-pipe (the FILED stg-freeze, docs/bug-pipe-stg-freeze-2026-08-05.md), and
e2e-stadium-dup (the pre-existing FIND/PLACE searching-livelock = bug-ledger
#2 = the V4 family's small-N tail). The first three are ONE redundancy-lane
investigation; the fourth dissolves if the V4 fix lands. The list may only
shrink; promotion is a human call on a measured rate.

`docs/bug-ledger-2026-08-05.md` has 9 entries incl. the stale-vpw room split
(#1), rotation-costs-a-ring-window (#9, a healing-laws design question), and
the iOS in-app-viewer camera question (#7).

## Box + harness facts (paid for tonight)

- All five boxes synced/clean at the release tree. Gate recipe unchanged:
  nvidia-laptop, node 22 PATH, `MEET_CHROME=~/.cache/ms-playwright/
  chromium-1193/chrome-linux/chrome`, reap BOTH browser binaries first.
- chromium-1193 = Chrome 140: NO RTCRtpScriptTransform, NO H.264. penguin's
  /opt/pw-browsers/chromium-1194 HAS H.264 (build flavor, not version).
  Capability-shaped reds: check the ENGINE first. e2e-pipe uses
  `findChrome({ignorePins:true})` deliberately.
- behavior battery in fleet mode needs servers bound 0.0.0.0 (`python3 -m
  http.server 8099 -d site --bind 0.0.0.0`; `RELAY_HOST=0.0.0.0 node
  test/servers/relay-local.js`) or scenarios refuse with "stack unreachable".
- ssh to every box drops mid-command constantly: launch detached work
  IDEMPOTENTLY (pgrep-check-then-setsid pattern), verify with a second ssh,
  never trust one round trip. Bracket every pgrep/pkill pattern.
- The monitor bot (raspberrypi tmux gifos-meet) runs the fixed edge; daily
  recycle 17:25Z. The moto was rebooted and is healthy. Watch the monitor
  room for fork recurrence (ledger #6) — none since the fix.
- Nathan's Telegram via the telegram skill (openclaw on raspberrypi,
  target 1511297360); delivered summary msg 14867 this morning.

## Standing directives from Nathan (this session)

- Full use of all boxes (penguin included); reboot restores their services;
  parallelize hard.
- NO BANDAIDS, NO BACKWARD COMPATIBILITY — root-cause fixes only; the cut
  is the flag day.
- The scale docs matter to him: he asked specifically whether the O(N)
  flood is gone. It is not. V1-V3 are the debt; V4 is the keystone.

## Addendum (same morning): the demo guard now exists

`test/browser/e2e-knock-first.js` — gUM overridden to never settle (the
iPhone shape, no iPhone needed): entrant must seat, be seen (occ>=2), stay
honest, and attach media on a late grant. 4/4 green on the fixed boot,
3/4 red against the pre-fix boot (negative control). Auto-gated.
Remaining iOS-fidelity gaps stay ledgered (#5 real-WebKit pipe, #7 in-app
webview camera): a REAL-Safari/WebKit lane is still the honest next step for
platform coverage.

## Addendum 2: WebKit-on-Linux, measured (the iOS coverage question)

Playwright WebKit (webkit-2311, Safari 26.5 engine) is INSTALLED on penguin
(host deps apt-installed) and probed against the site: getUserMedia YES,
RTCPeerConnection+DC YES, H.264 YES (the codec the chromium pin lacks —
complementary), canvas.captureStream YES, RTCRtpScriptTransform NO (absent
in the Linux port — ledger #5 mesh-pipe-on-Safari is NOT coverable here;
only real Apple builds expose it). API quirk: rejects Chromium permission
names ('camera') in newContext. iOS Simulators are macOS-only, no Linux
route. Real-iPhone-from-Linux: full automation needs macOS, BUT an iOS
Shortcuts personal automation (schedule -> Open URL -> the test room) plus
the monitor bot asserting from the MESH side = a zero-automation real-iOS
daily smoke, the moto-keeper pattern exactly. Next steps if pursued:
(1) wire a small webkit lane — e2e-knock-first + a 2-party join under
pw.webkit; measure whether a full mesh join completes there before
promising more; (2) a spare iPhone + Shortcut when hardware exists;
(3) BrowserStack only if those prove insufficient.
