# Handoff — 2026-07-20 (session 5: E5 refined, Q2 confirmed, H-CHAIN partial)

Continues `docs/handoff-2026-07-20d.md`. Written to be read cold.

## Nathan's decisions (locked)

| Item | Decision |
|---|---|
| **Priority** | **E → D → A → B** (compaction, H-CHAIN, loss wedge, reunion) |
| **E / Q2** | Self-duty only: each leaf asks parent/peers to place *itself* better |
| **A** | Park behind E/D — they should not make A worse |
| **A4 founder-death** | Closed by design: genesis lives on the relay; founder leave is ordinary |
| **B / E5** | **§1** friend-relay among co-members of one chosen meeting — OK. **§2** newcomer who can *see two meetings* gets **pick one** (R5) — **never** auto-bridge/merge (attacker-shaped sole witness). No paid media/data relay. |

## What shipped this session

### Docs / law
- `docs/healing-laws.md` — E5 split into §1 / §2; R5 aligned; Q2 self-duty; H-CHAIN partial LIVE; A4 closed in roadmap
- `docs/roadmap.md` — priority + item status
- `docs/media-plane.md` — friend-relay vs merge

### E5§1 drill
- `test/drills/e2e-peer-relay-reunion.js` — ICE-split co-members first, then third co-member of **same** room → "via Hub"
- **ALL PASS on nvidia** (not a two-meeting merge test)

### E / Q2
- Already LIVE; `test/sim/repro-compaction.sh` **GREEN** re-verified
- Added to `test/batteries/join.sh`

### D / H-CHAIN (partial)
| Piece | Status |
|---|---|
| Multi-level **admission** devolution (vacated admitter → col 1…C−1) | sim + mesh.js |
| Multi-level **reactive left-pack heal** (first rightward occupied with empty intermediates) | sim + mesh.js |
| `test/sim/repro-hchain.sh` | **GREEN** (heal dual-leave, admit dual-leave, head-only control, silent ring-hold) |
| `test/sim/repro-headless-row.sh` | **GREEN** (no regression) |
| Vertical recursion / S1 column clique / Q5 audit | **not yet** |

**Pitfall:** gating FIND hand-off on `firstHandLive` **breaks** headless-row silent-death control (fast-tracks past H1-S1). Devolution is **only** on vacated `occ`, not silence.

## Still open (order)

1. **H-CHAIN remainder** — vertical recursion when down-child healer is dead; S1 column devolution; Q5 small-N audit; full `test/sim/sweep.sh` soak with H-CHAIN on.
2. **Loss wedge (A)** — still Nathan-deferred; re-measure after more H-CHAIN.
3. **B §2 UI** — pick-one when newcomer sees two meetings (product/UI; law written).
4. **E soak** — home-LAN / live compaction demo optional.

## Ops
- nvidia + node 22 + real Chrome for browser drills
- `site/` auto-deploys; mesh.js change is control plane — run join `--quick` or full before relying on production
- Commit early on main and push

## Where to start next
1. Finish H-CHAIN vertical + Q5, sweep green.
2. Optionally wire E5§2 pick-one UI if a greeter-visible dual-home repro exists.
3. Do not freestyle loss wedge without Nathan.
